# BRIEF — Around The Grounds Tree Service demo build

## Prospect facts (REAL, verified — do not invent)
- **Business:** Around The Grounds Tree Service
- **Phone:** (864) 770-5067
- **City:** Anderson, SC
- **Services:** Tree removal, tree trimming/pruning, 24/7 emergency tree service, stump grinding
- **Angle:** No website. Emergency/24-7 tree service = "customers find a competitor at 2am, not you" angle.
- **Review source:** Nextdoor recommendations (fair pricing + efficiency for removals) — use honest placeholder reviews, NO fabricated quotes.

## Trade = TREE SERVICE (transform trade? NO — no before/after visualizer required. Tree is not a visual "transform" like concrete/pw/painting.)

## Assets already in this directory (USE THESE, do not regenerate)
- hero.mp4 (tree trimming crew, 1280x720, 6s) + hero-poster.jpg (true first frame)
- svc0.jpg (tree removal — felled trunk), svc1.jpg (trimming — arborist in canopy), svc2.jpg (stump grinding), svc3.jpg (wood chipper cleanup)

## HARD RULES (apply ALL — do NOT omit any)
1. WORK FROM THE GRAFT GRAPH — use `graft map`, `graft grep "<term>"`, `graft ask "<symbol-or-file>"` to locate the handyman/landscaping template + deploy conventions instead of re-reading the repo. Self-certify graft-used=YES.
2. HERO = full-bleed video (hero.mp4) with dark scrim + readable headline. Poster = hero-poster.jpg (the video's true first frame).
3. PREMIUM DEPTH CARDS on a dark gradient section — light cards rgba(255,255,255,0.92), backdrop-filter blur, rounded 18-22px, dark titles + green gradient icon badges. (Cards hold TEXT — so make text-card backgrounds FULLY OPAQUE #fff and DO NOT put backdrop-filter on text-bearing cards. See crisp-text rule.)
4. EVERY image distinct — 4 service cards = svc0..svc3 (4 unique tree-service photos), no reuse across sections.
5. NO SVG, NO fabricated reviews (honest "coming soon" placeholders), NO pricing section, real phone (864) 770-5067 in tel: links.
6. ⛔ CRISP TEXT ON WHITE — body/card text = font-weight:400 (NOT 300), muted grey = #555, light grey = #777. Text-bearing cards = opaque #fff, NO backdrop-filter on them.
7. QA at desktop 1280 + mobile 390 (no text overlap, no clipped CTAs).

## Output
Build a complete single-page index.html (all CSS inline in <style>, all copy on-trade tree service). When done, run a grep proving: phone 770-5067 present, no "B&M"/"Fountain Inn"/"804-8883", no placeholder text.

SELF-CERTIFY at the end: "graft-used=YES/NO, hero-video=YES, poster=first-frame=YES, premium-cards=YES, distinct-images=YES, crisp-text-400-weight=YES, no-fabricated-reviews=YES, checked-1280&390=YES."
