# Builder Brief — Zeroscapes CTA fix (final content correction)

You are the BUILDER agent. Fix the CTA section on `/mnt/d/LocalLaunch/builder-work/zeroscapes/index.html`. 

The CTA section (class `cta-section`) currently ends with a button/CTA that says **"See Your Online Presence & SEO Score →"** — that is the WRONG call-to-action for a build/rebuild demo (it's the audit pitch's CTA, and it doesn't belong here). For a landscaping build demo, the CTA should drive a phone call or free estimate.

## Fix
Replace that CTA link with a **"Call for a Free Estimate"** button that uses the real phone: `(864) 473-5530` with `tel:+18644735530`. Use the same button styling as the primary CTA buttons elsewhere on the page (so it matches). If the CTA section has room, also show the phone number as text next to/below the button.

Keep the "Claim Your Site / This is yours, Zeroscapes" heading and copy. Only change the CTA button.

## Verify
- The CTA section button now reads "Call for a Free Estimate" (or similar) with the phone number, linking to tel:+18644735530. No leftover "See Your Online Presence / SEO Score" text.

## Output
Report concisely: the new CTA text + link you set.
